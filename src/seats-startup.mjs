/** Re-export so the Ink entry has one import for the splash and the roster. */
export { loadSeats, checkSeat, renderSeats } from './seats.mjs'
export { renderStartup, renderHeader, resolvePalette, PALETTES } from './startup.mjs'
